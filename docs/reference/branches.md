# Branches — every one this repository has had

**Read it to place a branch in the project's history**: what it was for, when it opened, when it
landed, and which milestone and phase it belongs to. One line each, newest first.

**Five of its six columns are generated from git by `../procedures/branch-index.py`.** Only the last
one is written by a person. That split is the whole design and it is argued in
`../method/IDM-001-git-branching.md` — a hand-maintained list of merged branches was refused there,
correctly, because it drifts. A derived one cannot.

**Re-run the script at every merge — after the merge commit, never inside it.** Merge `--no-ff`,
then `python3 docs/procedures/branch-index.py --write`, then commit the table on the trunk. The new
row names the merge hash, so it cannot exist before the merge and amending the merge to hold it would
change the hash the row just recorded. `--check` exits 1 when the table no longer matches git, which
is the only thing standing between this file and the failure it was written to avoid.

## What this file is not

**It does not replace a phase note.** The permanent record of a phase — its plan, what it expected,
what it found, its evidence — is `milestone-N-*/phase-N-*/`, and each milestone's `README.md` reads
its own phases in order with a richer sentence than fits in a table cell. This is an index across all
of them, which is the one thing neither of those can be.

**It does not hold in-flight branches.** `IDM-001` gives those to `../status.md`, with the tree state
and the next action, and this file would be a second and staler home for the same thing. A row here
says what a branch *did*, which is not knowable until it is done. The script prints unmerged branches
as a note under the table rather than tabling them.

## Reading the table

| You see | It means |
|---|---|
| `~2026-07-27` | **Fast-forwarded.** The fork point did not survive, so this is the *earliest possible* opening date, not the known one — see below |
| *fork lost* | Same three branches: there is no second parent to take a merge base against |
| *fast-forward*, tip `fc65233` | No merge commit exists. The branch's tip sits directly on `main`'s first-parent line |
| `from 2a4e186` | The **merge base** of the two sides. A branch that pulled `main` in mid-flight moves this forward, so it is not always where the branch began |
| `—` in **M** or **Phase** | The branch belongs to no milestone, or to a milestone without being a phase. Both are legitimate: `IDM-001` makes the phase number orthogonal to the prefix |

<!-- generated by procedures/branch-index.py — do not edit between these markers -->

| Branch | Opened / forked at | Merged / commit | M | Phase | What it was for |
|---|---|---|---|---|---|
| `docs/branch-index` | 2026-08-21<br>from `5352d0d` | 2026-08-21<br>`3233fb7` | 2 | — | Two things. **The branch index** — this table, the script that generates it, and the `IDM-001` amendment that admits a derived index where a hand-maintained list was refused; enumerating refs found `docs/add-claude-md`, unrecorded for twenty-five days. **And four method items**: `IDM-007` on raising a concern where it will be read, `IDM-008` on the register, the read-by-section rule, and the forward-only `notes.md` split. |
| `docs/idm-and-claude-md` | 2026-08-20<br>from `2a4e186` | 2026-08-21<br>`5352d0d` | 2 | — | The two milestone playbooks moved out to `IDM-005` and `IDM-006`, the closing one **repaired as it moved** — five of its seven decided steps were missing. Forked from `feat/phase-10-body-store` rather than `main`, and having nowhere to record that is what added `IDM-001`'s third row. |
| `feat/phase-10-body-store` | 2026-08-18<br>from `d885b2f` | 2026-08-21<br>`32c26bb` | 2 | 10 | The body store built: content-addressed blobs, the byte-bounded queue and its worker thread, dictionary training and retraining, and `reference/corpus.md`. Thirty-three tasks, `make test` 310. The largest branch in the repository at 63 commits. |
| `docs/prompt-refresh-after-idm-001` | 2026-08-17<br>from `b0d5ed6` | 2026-08-17<br>`d885b2f` | 2 | — | Refreshed `prompt.md` for `IDM-001`'s new rule, and for a gap it had carried from the start. |
| `docs/status-shape-one-row-per-milestone` | 2026-08-17<br>from `4502cd8` | 2026-08-17<br>`b0d5ed6` | 2 | — | Replaced `status.md`'s per-phase table with one row per milestone. The per-phase merge hashes were not lost — each keeps two to six homes in the archive. |
| `docs/idm-001-status-placeholders` | 2026-08-17<br>from `b0abc03` | 2026-08-17<br>`4502cd8` | 2 | — | Generalised `IDM-001`'s closeout rule from the `Merge commit` row to **any** statement of unfinished state, after the narrow wording was obeyed as an instance and missed twice in one phase. |
| `docs/phase-9-corpus-gate` | 2026-08-17<br>from `97f6563` | 2026-08-17<br>`b29d502` | 2 | 9 | The corpus gate: 73 bodies captured over three runs, a held-out dictionary reaching 12.10×, and **`EPD-003` decided** — bodies are archived, fine-tuning dropped. Named Milestone 2's central claim. |
| `chore/tracked-allowlist-make-sync-and-clean` | 2026-08-17<br>from `e3205a4` | 2026-08-17<br>`97f6563` | 2 | — | Allowed all eight `make` targets in the tracked settings file rather than six. One commit; the smallest branch in the repository. |
| `docs/phase-8-method-and-guardrails` | 2026-08-17<br>from `6253cbc` | 2026-08-17<br>`22a6d20` | 2 | 8 | Opened Milestone 2 and built the `method/` tier: `IDM-000`–`IDM-003`, the branching rules unified from two homes that disagreed in seven places, and the tracked permission allowlist. **The first branch to carry a phase number on a non-`feat/` prefix.** |
| `docs/milestone-boundary-restructure` | 2026-08-15<br>from `9dd907e` | 2026-08-17<br>`9c30924` | 1 | 7 | The documentation restructure — the archive, the reference tier, the manual, and `CLAUDE.md` cut from 337 lines to 188. **The one phase whose folder and branch slugs disagree**, which is IDM-001's standing exception. |
| `feat/phase-6-review-and-cleanup` | 2026-08-07<br>from `f9f6d83` | 2026-08-07<br>`532dc86` | 1 | 6 | The review. Fifteen of sixteen quoted measurements reproduced to the digit; seven items fixed in about forty lines; two proposed cuts refused with the measurement that refused them. |
| `feat/phase-5-config-and-timeouts` | 2026-08-07<br>from `74f38d6` | 2026-08-07<br>`c8401e9` | 1 | 5 | The three credential modes, built and carried live against an authenticated LM Studio, and `read_timeout` made per backend — which unblocked ruling out silent trimming below the context boundary. |
| `feat/phase-4-lmstudio-parity` | 2026-08-06<br>from `cc65aed` | 2026-08-06<br>`50444c5` | 1 | 4 | What LM Studio supports, measured instead of guessed. **Nothing was rejected**; the gaps are things accepted and not honoured. Found the router's read timeout reachable by ordinary traffic. |
| `feat/phase-3-failure-handling` | 2026-07-31<br>from `acb399f` | 2026-07-31<br>`cc65aed` | 1 | 3 | The failure taxonomy — and the finding that **four of its five planned items were already built** by Phase 2. Measured what Claude Code does with an error: retries a 502 ten times, ignores a mid-stream `error` event. |
| `docs/epd-index-and-corpus-proposal` | 2026-07-31<br>from `4d7d7f6` | 2026-07-31<br>`acb399f` | 1 | — | `EPD-000` as the index and `EPD-003` on body capture: the proposal Milestone 2 was later built from. Documentation work belonging to the milestone without being a phase. |
| `feat/phase-2-observability` | 2026-07-30<br>from `8d335ab` | 2026-07-31<br>`4d7d7f6` | 1 | 2 | The rotating log and the twenty-column CSV. Its step 6 session — long, both backends, subagents, an interrupted response — is the evidence half the project still rests on. |
| `feat/phase-1-proxy` | ~2026-07-29<br>*fork lost* | *fast-forward*<br>tip `8d335ab` | 1 | 1 | Dispatch working end to end: the `HEAD /` probe, `POST /v1/messages` by model name, a catch-all, byte relay and streaming. Verified in a real session on 2026-07-29. |
| `feat/phase-0-skeleton` | ~2026-07-27<br>*fork lost* | *fast-forward*<br>tip `fc65233` | 1 | 0 | The uv project, config loading and the CLI. No notes file — its record is a section of `milestone-1-core/implementation-plan.md`. |
| `docs/add-claude-md` | ~2026-07-27<br>*fork lost* | *fast-forward*<br>tip `1636024` | — | — | The repository's first documentation — the README, `CLAUDE.md`, the design decisions and observability spec, and the phased implementation plan. **Predates Phase 0 and the milestone scheme**, so it belongs to neither. |

<!-- end generated -->

## Three things the table shows that no other document did

**There are three fast-forwarded branches, not two.** Every document that mentions this says *"Phases
0 and 1 were fast-forwarded"* — `../status.md`, `../milestone-1-core/README.md`, `IDM-001` — and each
is correct, because each is talking about *phases*. But `docs/add-claude-md` is a third, and it
predates Phase 0: five commits on 2026-07-27 that put the README, `CLAUDE.md`, the design decisions
and the first implementation plan into the repository. It appears in no document before this one.
Nothing was lost and nothing was mishandled; it simply belonged to no phase, and until `IDM-001`
gained its third row on 2026-08-21 a branch belonging to no phase had nowhere to be recorded.

**Nineteen branches, and eight of them carry no phase number.** Better than two in five is work
that is real, merged, and outside the phase sequence. That is the ratio behind `IDM-001`'s
"orthogonal" rule, and it is the argument against the reflex that a phase note is where branch
history lives: a scheme that only records phases would be missing eight of these rows.

**The two ends of the size range are both instructive.** `chore/tracked-allowlist-make-sync-and-clean`
is one commit; `feat/phase-10-body-store` is sixty-three across four days. Both were merged `--no-ff`,
which is why both are visible here at all.

## Why this is in `reference/`

**It is worth stating, because the tier's own admission test appears to refuse it.**
`README.md` asks: *would this fact still be true if the phase that found it had never happened?* — and
a table made of phases seems to answer no.

It passes on the second reading, which is the right one. The test asks whether a fact **depends on a
phase's plan, its ordering, or what it expected versus found** in order to be understood. These rows
do not: a branch's name, dates, hashes and purpose are true as read, from any distance, and they stay
true after the milestone closes and its folder stops being live. The alternative homes were both
worse — a milestone archive can only hold its own branches, and `../status.md` holds state, which this
deliberately is not.

The tier's other rule is the stronger argument: **one file per subject, each with a nameable trigger.**
The trigger here is exact — *you have a branch name, or a merge hash, and want to know what it was* —
and no other file answers it.

---

*Written 2026-08-21 on `docs/branch-index`. The generated block is produced by
`../procedures/branch-index.py`; the counts in prose above are read off the table and change with it,
so re-derive them rather than relaying them if the table grows.*
