# Phase 12 — evidence

**Nothing is frozen here yet, and that may remain true.** *Written 2026-09-02, at the phase's
opening, because `../../../README.md`'s phase template asks for this file and a phase whose evidence
is observational rather than artefactual should say so rather than leave an empty directory.*

**What this phase produces is mostly observation, not artefact.** Group B drives an installed tool
and records what it does; those records belong in `../notes.md`. Phases 9 and 11 froze real slices —
a corpus, a redacted index — because a later reader needed to re-run something against them. Nothing
here is yet known to be of that kind.

**What would earn a place here**, if it happens:

- **The listing that proves the config template shipped inside the wheel.** Task 7's claim cannot be
  re-checked from prose, and the machine that could refute it — one without this repository — does
  not exist here. A listing of the installed package's contents is the nearest available proof.
- **The transcript of an installed run in a directory that is not the repository**, if Group B finds
  anything that contradicts the plan.

*If neither happens, this file is the record that neither happened.*
